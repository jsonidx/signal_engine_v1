# Backlogs

Backlog documents in this folder are forward-looking implementation notes that
do not yet need the full task template in `docs/tasks/`.

Use this folder for:

- migration plans
- provider replacement notes
- architectural debt inventories
- future integration sketches

When a backlog item becomes ready for implementation, split it into one or more
task tickets under `docs/tasks/new/`.
