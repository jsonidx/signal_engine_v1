[build]
builder = "NIXPACKS"
buildCommand = "pip install -r requirements-bot.txt"

[deploy]
startCommand = "python3 scripts/telegram_bot.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
